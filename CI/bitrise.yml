format_version: '11'
workflows:
  primary:
    steps:
    - script@1:
        title: Placeholder
        inputs:
        - content: |
            echo "Configure Xcode project, signing, and TestFlight deploy after credentials are available."
